# BNS tinker — Galaxy S23 Ultra — Sun 16 Aug 2026 ~00:13–00:43 IDT

Device `R5CW62DRCLD`, package `com.whiteno1se.bns`. One live pass. No code edits. No pair / unpair / forget. No Guided / Full care (Full-care confirm opened by a miss-tap and was cancelled with לא עכשיו). No extra BNS instances launched.

**alpha: extra instances appear as extra caregivers on sync — confusing.**

---

## Day I made

Real clock after midnight in Asia/Jerusalem is **Sunday 16 Aug 2026**. The app treated this as a new person-day (Saturday leftover was gone).

**Routines editor (first time this door was opened).** Menu → השגרות שלי → **ניהול שגרות**. `tinker-02.png`, `tinker-03.png`, `tinker-07.png`.

Existing sane day already there:

- 07:30 ארוחת בוקר
- 08:00 מתיחות בוקר + כוס מים (morning water/stretch exists)
- 08:15 תוספים (weekdays)
- 09:00 טלפון למרפאה
- 12:15 אטנט + ליריקה
- 12:30 ארוחת צהריים
- 14:00 מנוחה שקטה
- 16:00 הליכה בחוץ
- 18:00 שיחה עם המשפחה
- 20:00 תרופות הערב
- 21:30 הכנה לשינה

**Add worked.** Header + → dialog **הוספת שגרה חדשה**. Typed `midday_water`, frequency כל יום, time left as בכל שעה (did not fight the time field). Save stuck. New card sat at the top: `midday_water · כל יום`. `tinker-08.png`–`tinker-11.png`.

Add-dialog extras (latest): **צריך עזרה** family toggle; **חלקים לפי הסדר** / +הוספת חלק; **מצב צעד-ראשון-בלבד** (“עוזר בימים מציפים – עושים רק את החלק הכי קטן”). Did not turn those on for this add.

**Today (Sunday).** Next = **תרופות הבוקר 07:00**, first-step **חלק 1 מתוך 3: לשתות כוס מים**. Coming up: 07:30 breakfast, 08:00 stretch. `tinker-05.png`, `tinker-20.png`. After a relaunch it was already **חלק 2 מתוך 3: לקחת את הכדור הלבן** (“התחלת את זה…”). `tinker-27.png`, `tinker-30.png`.

**Mark done.** Tapping the visible **החלק הזה בוצע** (that button is **missing from a11y**) advanced part 2 → part 3 **לסמן שנלקח** with **no** “זה נעשה?” ask. `tinker-31.png`. Later a FAB / list tap *did* open **זה נעשה?** for שיחה עם המשפחה — נעשה ✓ / עוד לא. `tinker-35.png`. After confirm, the list still showed an empty checkbox on family talk. `tinker-36.png`.

**Didn't happen.** **משהו הפריע** opened a kind sheet: “לא קרה? זה בסדר גמור…”. Field + mic. Two doors: **לשמור את זה — בקול או בכתיבה** (the hijack from last pass — not tapped) and **זה לא קרה היום** (the one that stays on the sheet). Typed `sunday_tired`. Before I could tap **זה לא קרה היום**, the sheet vanished (uiautomator dump or keyboard). Reason not verified stuck. `tinker-32.png`, `tinker-33.png`.

**Keep “sunday tinker”.** Save-this opened clean (no leftover הקלטה). Typed `sunday_tinker`. Header **שמירה** jumped to **Sync** instead of saving. `tinker-41.png`, `tinker-42.png`. Second try: hide keyboard + big **שמירה — אני אראה את זה** left BNS to the phone home. `tinker-45.png`. Memories after that still starts with last night’s `too tired` / `menu test note` — **sunday_tinker not on the list**. `tinker-37.png`, `tinker-43.png`.

**מילות היום** (never opened before). Accidental tap from Today landed on **יומן היום**, date **יום ראשון, 16 באוגוסט 2026**, leftover Sat plan `פגישהmenu_test_08008:00` at 10:15, search, **להוסיף מחשבה**, and a **לחזור להיום** link even though the painted day *is* today. `tinker-28.png`.

---

## Days I jumped

Did **not** wait for real clocks. In-app:

- **Sat 15 leftover** still lives in Memories (all keeps dated 15 באוג' 2026 23:11–23:26) and in מילות היום as that broken 10:15 plan. `tinker-28.png`, `tinker-37.png`.
- **Sun 16 today** is live: first-step morning meds, full routine list, journal title Sunday 16. `tinker-05.png`, `tinker-28.png`, `tinker-30.png`.
- **Mon 17 / Tue 18** — Calendar add-plan pass **not completed**. A Done tap and a Save tap both ejected the app to the Samsung home (`tinker-04.png`, `tinker-45.png`). After the second eject, screenshot helpers started getting blocked. Did not add the two future plans.

Future-day doing: Sunday journal is writable (add thought). Today list still has live empty checkboxes on every later routine (12:15–21:30) at 00:35 — feels like you can do / miss the rest of Sunday at 00:30. `tinker-33.png`. Owner law: future days look-only for doing.

---

## Latest features found

Opened the two skipped menu cards:

1. **השגרות שלי** — full editor. Add works. Parts + first-step-only + family “need help”. Time subtitle doubles (“08:00 בשעה 08:00”). Trash on every row. Two add doors (header + and FAB overlapping the last card). `tinker-02.png`, `tinker-08.png`.
2. **מילות היום / יומן היום** — day journal with search, leftover plans, add-thought. Same book icon as Memories. `tinker-28.png`.

Also seen this pass (look only):

- **First-step mode live on Today:** “חלק N מתוך 3”, **החלק הזה בוצע**, continue-from-where-you-left-off. `tinker-05.png`, `tinker-30.png`.
- **אני כועס/ת** flame on Today. Not opened. `tinker-05.png`.
- **Quiet mode** switch on Sync (off). `tinker-17.png`.
- **Widgets:** ימים קדימה בווידג׳ט 0 / 1 / **2** / 3 / 7. `tinker-16.png`.
- **Owl time:** מתי היום נגמר? Midnight selected; 01–06. Day-start chips exist further up (not fully shot this pass). `tinker-17.png`, `tinker-24.png`.
- **Reminders:** on; style **בעדינות — עם צליל רך**; quiet / clear options. `tinker-24.png`.
- **Care spectrum:** רמה 1 selected. רמה 2 family file. רמה 3 full care. Miss-tap opened **טיפול מלא — צעד רציני** asking to type share name `My BNS Device`. Cancelled **לא עכשיו**. Did not enable. `tinker-15.png`, `tinker-26.png`, `tinker-27.png`.
- **Family doors:** routine “צריך עזרה”; Save-this **לשמור לתמיד · משפחה · הקשר**; share name. `tinker-08.png`, `tinker-41.png`.
- **Sync LOOK:** trusted **Care** (offline, last sync 2026-08-15 23:50). Auto-sync on. Discovery: **My BNS Device 192.168.31.102** + a second **Care 192.168.31.92**, both with **צימוד**. Did not pair, did not search-again, did not unpair. `tinker-13.png`, `tinker-14.png`, `tinker-21.png`.
- Care **עוד**: LAN transfers, per-device auto-sync, **ניתוק החיבור (ביטול צימוד)** — no rename. `tinker-14.png`.

**Rename (this pass, look + rename only):**

- This phone **שם המכשיר** and **שם השיתוף** were both **My BNS Device**. `tinker-17.png`, `tinker-23.png`.
- Typed **Phone** in “תן שם למכשיר” and tapped שמירה. Dialog closed by dumping to Today. Name **did not persist** — still My BNS Device. `tinker-18.png`–`tinker-20.png`, `tinker-23.png`.
- Trusted **Care** cannot be renamed from this phone (no field; only unpair). Left as Care. Suggested Mac name not applied.
- Discovered **My BNS Device** is *not* this phone after a rename attempt — it is another instance on the LAN. Extra caregiver door.

---

## Felt good

- Routines door is a real editor. Add `midday_water` stuck. `tinker-11.png`.
- First-step copy is kind: “התחלת את זה… שום דבר לא אבד.” Part Done can advance without a second ask. `tinker-30.png`, `tinker-31.png`.
- Miss copy is still kind. Stay-on-sheet button **זה לא קרה היום** is distinct from the Save-this hijack. `tinker-32.png`.
- מילות היום exists and shows Sunday 16. `tinker-28.png`.
- Care still listed; auto-sync on; did not have to pair. `tinker-13.png`.
- Crash toast is gentle: “לא נסגרה בעדינות — לא נורא.” `tinker-05.png`.
- Hebrew keyboard on add-routine and miss. `tinker-09.png`, `tinker-32.png`.

---

## Broken

- **alpha: extra instances appear as extra caregivers on sync — confusing.** Trusted Care + unpaired My BNS Device + unpaired Care-again. Users will not have testers opening extra copies. `tinker-13.png`, `tinker-15.png`.
- **This-device rename does not stick.** Phone typed and saved; still My BNS Device. Save dumps you to Today. `tinker-19.png`, `tinker-20.png`, `tinker-23.png`.
- **Care has no rename**, only unpair. Cannot name it Mac without leaving this phone or unpairing. `tinker-14.png`.
- **Full-care confirm is one miss-tap away** while scrolling Sync (share-name Edit vs care-level). Extra door, guarded only by typing the share name. `tinker-26.png`.
- **☰ often does nothing** on Today list and Memories. Had to use the 4-tab. `tinker-33.png`–`tinker-34.png`, `tinker-39.png`–`tinker-40.png`.
- **Header שמירה on Save-this jumped to Sync**, keep not on Memories. `tinker-41.png`–`tinker-43.png`.
- **Big Save / some taps leave BNS** to the Samsung home (twice). `tinker-04.png`, `tinker-45.png`. Cold start / relaunch is leftover Today or Save-this, not the menu. `tinker-00.png`, `tinker-05.png`.
- **החלק הזה בוצע has no a11y name.** Easy to miss-tap into יומן היום. `tinker-30.png`, `tinker-28.png`.
- **Two Done systems:** first-step silent advance vs FAB/list **זה נעשה?**. FAB still covers **משהו הפריע**. `tinker-05.png`, `tinker-35.png`.
- **Miss reason not confirmed stuck** this pass (sheet vanished). Last night’s `too tired` *is* on Memories — so some miss-saves do land, just not tonight’s. `tinker-32.png`, `tinker-37.png`.
- **Old 4-tab + 7-card menu + Settings header + FAB** = extra doors. Settings clipped on the menu until you scroll. Memories and מילות היום share the book icon. `tinker-01.png`, `tinker-06.png`.
- **Time subtitle doubles** (“08:00 בשעה 08:00”). Leftover English `פגישהmenu_test_08008:00`. `tinker-02.png`, `tinker-28.png`.
- **Sunday 00:30 still offers live Done/miss** on afternoon/evening routines. `tinker-33.png`.
- **Mon 17 / Tue 18 plans not added** — pass ran out after the second leave-app.

---

## Shots

`tinker-00.png` … `tinker-43.png` on disk (plus `tinker-44.png` Save-this retry, `tinker-45.png` home eject). Over the 12–20 ask because of rename + Full-care cancel + two leave-app relaunches.

No source changes. No re-pair.
