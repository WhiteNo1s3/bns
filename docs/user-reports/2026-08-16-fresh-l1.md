# Fresh instance / Level 1 — 16 Aug 2026 00:27–00:40 IDT

Phone: Galaxy S23 Ultra `R5CW62DRCLD`, package `com.whiteno1se.bns` only. Force-stop then monkey-launch. No source change. No clone. No OS permission taps. One phone instance.

Closed extra Mac test copies (`BNS-Person`, `BNS-Care`). Xcode debug `BNS.app` would not die. Did not launch Windows/Mac BNS. Phone debug package force-stopped.

Shots: `fresh-00.png` … `fresh-22.png` (over the 12–18 ask because leftover dialogs kept stealing taps).

---

## What I did

1. **Fresh launch = Today, not leftover Save-this, not the new menu.** `fresh-00`. Title `היום • BNS`. Next card: `תרופות הבוקר` 07:00 part 1/3 (drink water). Soft-close banner: "בפעם הקודמת האפליקציה לא נסגרה בעדינות — לא נורא…" Force-stop is not a gentle close; the banner is honest. Last sync 15 Aug 23:50.

2. **Settings / Sync.** Care is the bonded friend. Offline: "לא בסביבה כרגע · סונכרן 2026-08-15 23:50 — יסתנכרן ברגע שיחזור". Auto-sync on. Pair strip still offers `My BNS Device 192.168.31.102` and later `Ben 192.168.31.92`. Did not pair. Did not tap unpair. `fresh-01`, `fresh-15`.

3. **Rename.** This phone is still **My BNS Device**. Share-name Edit exists (`שם השיתוף שלך` / `שם המכשיר הזה`, both `עריכה`). `fresh-03`. Could not finish typing **Phone** — Done dialogs and leftover miss-saves ate the taps. Bonded friend is named **Care**, not Mac. No rename-without-pair control on the Care card. Left it.

4. **רמת ליווי.** Stayed on **רמה 1** (selected). Copy read, not flipped:
   - רמה 1 עצמאי — הכול בידיים שלך, שום דבר לא יוצא מהמכשיר אלא אם תבחר.
   - רמה 2 המשפחה בעניינים — תוכניות שבחרת נכנסות לקובץ המשפחה.
   - רמה 3 ליווי מלא — האנשים שאכפת להם רואים הכול, כולל הרגעים הקשים.
   - רמה 4 מונחה — רק הרשימה. את היום בונה המלווה.
   `fresh-05`, `fresh-06`. Tapping L1 (already on) dumped me back to Today. Did **not** turn on 3 or 4.

5. **Menu.** ☰ opens the 7-card תפריט. Settings card clipped under the 4-tab. Tapping the clipped card hit the FAB instead. `fresh-08`, `fresh-11`, `fresh-12`.

6. **Memories.** `too tired` is first row (15 Aug 23:26) — last night's miss reason **did stick**. Also `menu test note`, `לא היה בא לי`, `בלחץ לעשות דברים`, `לא רעב`, `לא הולך בשבת`. Put-aside door exists (`דברים שהונחו בצד`). `fresh-20`.

7. **Mark done / tomorrow / routines / today's words.** Meds moved 1/3 → 2/3 → 3/3 while I was in Settings/Menu — I did not mean to mark them. "זה נעשה?" then asked about `שיחה עם המשפחה`. Calendar month is reachable (Sat 15 still lists `level1 plans`). השגרות שלי and מילות היום are menu cards; Settings collision blocked a clean open. Did not add a new keep this pass.

No OS permission dialogs. OS, skipped: none appeared.

---

## Share / not share

Level 1 copy already says nothing leaves unless I choose. That is the global "not share".

Per-item door exists on add-event: checkbox **המשפחה יכולה לדעת** — "נכנס לשיתוף המשפחתי — לדברים חשובים כמו תור לרופא…". Seen on the add-event sheet (prior dump + this app). I did **not** get two fresh events saved this pass (leftover dialogs). Could not screenshot ON vs OFF side by side on new items.

What I could confirm:
- L1 selected = default not-share (nothing leaves unless I pick).
- L2 is the "family file" door for plans I choose. Looked; did not switch.
- No "make the family file" export this pass. Did not export. Put-aside pile is a personal trash (3 days, can bring back), not the family file.

Two visual states I **did** get of the caregiver pipe:
- Care LAN **on**: "העברות ברשת הביתית מותרות. כבוי = עדיין מצומד, אבל שום דבר לא עובר לשום כיוון." `fresh-02`.
- That off-copy **is** "give them nothing while still bonded". Toggle tap missed and opened a leftover miss-save instead of flipping the switch. Left LAN on.

---

## Caregiver bond

Bonded friend: **Care**. Still paired. Offline tonight. Last sync 15 Aug 23:50. I gave them nothing new (L1, no new shared event, did not sync-now).

Doors I opened:
- Expand Care: LAN allowed (on), auto-sync with this device (on), disconnect/unpair (did not tap). `fresh-02`.
- "Give them nothing" = LAN off while still paired. Read the copy. Did not successfully flip it.
- Pair offers for leftover Mac identities (`My BNS Device`, `Ben`). Left them. Extra instances still advertise as strangers.
- Could not rename Care → Mac without pairing. Did not start a new pair.

Intervene / full care: L3 and L4 copy is on the same list as L1. Opened the list, read it, stayed on 1. Prefer that over flipping locked modes.

---

## Annoyed (app only, not OS)

- **Force-stop leftover is a banner plus a trap.** Today tells me the last close was not gentle. Then leftover miss-saves (`too tired` / `הליכה בחוץ` / "לא היה בא לי") keep coming back as sheets on top of Settings. `fresh-00`, `fresh-12`, `fresh-16`.

- **Nav eats Done.** Morning meds went 1/3 → 2/3 → 3/3 while I was in Sync and Menu. I did not tap `החלק הזה בוצע`. Someone with a sore brain will think they took the pill. `fresh-00` vs `fresh-07` vs `fresh-09`.

- **FAB sits on the new menu.** Settings card is clipped. Tap lands on `לסמן את הבא ✓` and you get "זה נעשה?" for a leftover (`הכנה לשינה` + note `too tired`, then `שיחה עם המשפחה`). Three Dones, two maps, one finger. `fresh-11` → `fresh-12` → `fresh-13`.

- **Tapping רמה 1 (already on) kicks you out of Settings** onto Today. Extra door. `fresh-06` → `fresh-07`.

- **Put-aside after a miss-save.** "להניח את זה בצד? זה מחכה באשפה 3 ימים." Primary button is the destructive one. I backed out. `fresh-16`.

- **Device still called My BNS Device.** Name field exists; the path to it is a long Settings scroll under Care / history / widget days. Pair strip shows the same dummy name as a stranger. Care is Care, not Mac.

- **Calendar vs Today still lying.** Month list for Sat 15: `level1 plans` all-day. Sunday Today: morning meds / breakfast / stretch. Same weekend, two stories.

- **Hebrew thought:** נמאס לי שהתפריט והסימון גונבים אחד את השני.

---

## Felt good

- Cold start was **Today**, not a half-finished voice Save-this. Better than last night.
- Soft-close copy is kind. Nothing lost, it says, and Memories proves `too tired` stayed.
- רמה 1 copy is the right promise: nothing leaves unless I choose. I can bond Care and still give them nothing.
- LAN-off copy is clear: still paired, nothing moves. That door is for me.
- L3/L4 sit there as explanations I can read without flipping.
- Menu cards have titles and subtitles. I can see השגרות שלי and מילות היום even when I cannot land in them.
- Care is still my friend. Offline, not forgotten. Did not have to pair again.
- Miss-reason silk law held. Last night's `too tired` is the first memory.

