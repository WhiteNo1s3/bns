# CLAUDE.md

You are in the BNS / gBNS tree (`~/dev/gBNS`).

1. Read **AGENTS.md** — those laws win.
2. Read **changes.md** — that is the test brief (file-ear, system clock, plans in Next, later-today, person-day).
3. **GROK.md** is the same door Grok uses. Do not invent a second process.

Test pass: pick one block in `changes.md`, run `flutter test` on the files it names, answer only its live-test questions. Pass / fail / what you changed. Do not add features unless the owner asked.

Hard no: cloud, concurrent STT+record on Android, motion, unlabeled icons, shame, hardcoded timezone city, L3/L4/servers unless asked.

Iterate list (what to build next): **PROTOTYPER.md**.
