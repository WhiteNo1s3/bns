# Forks archive (2026-08-21)

The parallel trees `~/dev/gBNS` and `~/dev/grokBNS` (both remotes:
github.com/WhiteNo1s3/grokBNS) were retired on the owner's word ("the
folders and files that are not needed lets remove them"). Everything the
main loop ever read from them is kept here: the Prototyper's iterate list
(`gBNS-PROTOTYPER.md` — Eagered's notes live in it), the fork's seat files
and changelog, and each fork's UNCOMMITTED work as a text patch + file
list (binaries excluded). Their committed history stays on GitHub.
