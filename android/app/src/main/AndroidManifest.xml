<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- LAN-only sync (UDP discovery + TCP transfer on local Wi-Fi) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android's Wi-Fi chip DROPS broadcast/multicast packets to save
         battery unless the app holds a multicast lock. Without this the
         phone never hears another device saying hello (owner's test,
         2026-07-27: phone->PC worked, PC->phone was deaf). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Voice quick capture -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Gentle reminders (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Reminders must survive a phone restart — the boot receiver below
         re-schedules them. Without it a reboot silently eats every reminder,
         which for this community means a missed medication, not a missed
         ping. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- EXACT DELIVERY (owner as user, 2026-08-18: "I don't get the
         notifications"). Inexact alarms get batched and deferred by the
         battery police until they help nobody. BNS is an alarm-and-
         calendar app in the most literal sense — USE_EXACT_ALARM is the
         permission made exactly for this class, granted at install. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <!-- The wake alarm takes the whole screen like an alarm should. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Planting the wake into the phone's own clock app (its alarms
         survive anything, and its ringtones are where the songs live). -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="BNS">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- BNS file type association - open .bns files to import -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="content"
                    android:mimeType="application/octet-stream"
                    android:pathPattern=".*\\.bns" />
                <data android:scheme="file" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.bns" />
                <data android:host="*" />
            </intent-filter>
        </activity>
        <!-- BNS widget bundle: three home-screen widgets, all dirt simple -->
        <receiver
            android:name=".BnsTodayWidgetProvider"
            android:exported="false"
            android:label="BNS — Today">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_info" />
        </receiver>
        <receiver
            android:name=".BnsUpcomingWidgetProvider"
            android:exported="false"
            android:label="BNS — Coming up">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_upcoming_info" />
        </receiver>
        <receiver
            android:name=".BnsActionsWidgetProvider"
            android:exported="false"
            android:label="BNS — Quick actions">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_actions_info" />
        </receiver>
        <!-- flutter_local_notifications does NOT register these receivers by
             itself (checked in the plugin's own AndroidManifest, v17.2.4).
             Without ScheduledNotificationReceiver, every scheduled reminder
             (AlarmManager broadcast) lands on a receiver that doesn't exist —
             reminders simply never appear. The boot receiver re-schedules
             everything after a restart or an app update. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Speech-to-text: lets the app see the device's recognition service
             on Android 11+ (package visibility). On-device engine, no cloud. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <!-- The Waze door (system listening sheet) on Android 11+. -->
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH"/>
        </intent>
        <!-- Seeing the phone's clock app on Android 11+ (the wake plant). -->
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS"/>
        </intent>
    </queries>
</manifest>
